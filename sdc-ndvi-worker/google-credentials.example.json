{
  "_description": "Do not commit real credentials. In production, mount the real service account JSON as a secret and point GOOGLE_APPLICATION_CREDENTIALS to that path.",
  "type": "service_account",
  "project_id": "replace-me",
  "client_email": "replace-me@replace-me.iam.gserviceaccount.com",
  "private_key": "replace-with-secret-loaded-outside-git"
}
