import { CambiarPasswordComponent } from './cambiar-password.component';

describe('CambiarPasswordComponent', () => {
  it('exports the component', () => {
    expect(CambiarPasswordComponent).toBeTruthy();
  });
});
