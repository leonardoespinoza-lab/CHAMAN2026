# Angular usa environments en build time.
# Para staging/production configurar environment.prod.ts o generar archivo runtime antes del build.
CHAMAN_API_URL=https://<railway-sdc-api-cliente-url>
CHAMAN_WS_URL=wss://<railway-sdc-websocket-url>
CHAMAN_TILES_URL=https://<railway-sdc-api-cliente-url>/data
